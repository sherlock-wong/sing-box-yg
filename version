v26.7.25-vpnm.10

VPS Net Manager：项目已更名为 `sherlock-wong/vps-net-manager`。安装命令为 `vpnm`，服务名、UFW 注释、证书同步任务、Reality 临时文件和默认状态目录统一使用 `vps-net-manager`。

改进：Hy2 启用向导增加 UDP 端口跳跃选项，可保留、修改或关闭范围；跳跃范围会检查 UDP 监听冲突，并在写入配置前探测 iptables/ip6tables 的 NAT REDIRECT 能力。任一地址族不可用时会明确提示，两个后端均不可用时拒绝保存跳跃配置，原配置不变。

改进：从协议列表启用任一未启用协议时，统一进入端口向导，可选随机、自定义或保留预设端口；启用前会按协议展示 Reality SNI、WS Path、TLS 证书、Hy2 带宽或 AnyTLS Padding，并校验普通 TLS 证书覆盖域名。

改进：证书库视图会明确列出未启用的 Vless-Reality，并标注其不适用普通 TLS 证书库，同时展示 Reality SNI；普通 TLS 证书绑定仅适用于 Vmess-WS、Hysteria2 和 AnyTLS。

修复：MetaCubeX 规则库只提供会被替换的 `latest` 资源，旧资产编号会返回 404。安装时改为读取官方发布 API，要求 geoip.db 与 geosite.db 都提供官方 SHA-256 digest，并对实际下载文件再次校验；缺失 digest 时拒绝安装。

改进：Realm 菜单增加独立的启动和停止操作。启动会校验已安装状态及至少一条规则，并设为开机启动；停止保留规则，但停止服务并取消开机启动。

改进：证书库与协议绑定解耦。主菜单 `7` 只申请、导入、查看和维护证书；域名证书入库后不会自动修改协议。每个普通 TLS 协议在自身专项参数中从证书库选择所需证书。

改进：普通 TLS 协议修改证书域名时会筛选证书库中 SAN 覆盖该域名的有效证书，必须选择匹配项才能应用；协议操作菜单新增“查看当前协议配置”。

改进：首次安装新增“推荐默认 / 自定义初始配置”。默认随机分配未占用端口；自定义模式可在安装前一次设置各协议端口、Reality SNI、普通 TLS 域名和分享链接对外地址，并校验端口占用与重复。

改进：首次安装填写自定义普通 TLS 域名后立即检查证书库与已有 ACME 证书；无匹配项时必须先选择 ACME 申请或导入有效证书，之后才进入安装摘要。受信证书会入库并自动绑定本次选择的普通 TLS 协议。

修复：首次安装已经绑定受信证书时，不再重复显示“自签证书”建议；未安装时在主菜单明确标出依赖已安装服务的选项。

修复：首次安装的自定义普通 TLS 域名和受信证书只应用于实际选中的协议；其他协议保留独立的 `www.bing.com` 默认自签证书占位配置。

当前功能：按需管理 Sing-box 协议、可选 Xray-core Reality、Reality 目标扫描、证书库、AnyTLS、Hysteria2、Vmess-WS、WARP、TCP/BBR 与 Realm 端口转发。

范围：仅 VPS；不包含 Serv00、Hostuno、保活网页或多用户面板功能。
